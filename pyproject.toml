[project]
name = "faktory-renderer-environment"
version = "0.1.0"
description = "Pinned Python environment for the Faktory CadQuery renderer"
requires-python = ">=3.12,<3.13"
dependencies = [
    "cadquery==2.6.1",
]

[tool.uv]
package = false
